"""
Seeds a handful of mock devices into SQLite on first run, so the dashboard
looks like a real fleet even though only one device is a genuinely
Intune-enrolled VM.

TODO (Phase 5): insert seed rows.
"""
